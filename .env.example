# Only variables intended to be public belong here — anything prefixed VITE_
# is inlined into the browser bundle and is readable by every visitor.
VITE_API_URL=http://localhost:4000/api
VITE_SITE_URL=http://localhost:5173
